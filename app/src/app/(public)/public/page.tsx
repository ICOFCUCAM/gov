import { PublicObservatory } from '@/components/features/PublicObservatory';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Public Observatory' };

export default function PublicObservatoryPage() {
  return <PublicObservatory />;
}
